# ⚡ Python 并发 16 小项目学习清单 · Todo List（含 1 个选做前沿）

> 通过 16 个小项目（每项目 100-350 行代码）系统掌握 Python 并发：从线程与 GIL 地基，到多进程并行、asyncio 异步，再到死锁竞态诊断与执行模型选型，最终收拢为一个可切换执行模型的并发系统
> 约束：macOS Apple Silicon · Python 3.13 · 标准库为主 + aiohttp（已装）· 全部本机可跑、零云依赖
> 串联机制：知识体系型——阶段即能力等级（线程 → 多核 → 异步 → 诊断设计 → 架构综合），项目 15 为 🏁 综合串联项目，项目 16 为 ⚠️ 选做前沿
> 预计周期：5 周（每天 1.5-2 小时）

## 🤖 AI 辅助提示词速查

| 场景 | 提示词 |
|:---|:---|
| **开始一个新项目** | `我要开始 Python 并发项目「[项目名称]」，目标是 [核心功能]。请给我完整代码约 [行数] 行，单文件、零第三方依赖（如项目允许 aiohttp 则注明），含类型注解、中文注释和 main 演示，附验收命令。只输出代码。` |
| **排障** | `我的并发程序出现 [挂起/竞态/结果不一致]，现象：[描述]，输出/栈：[粘贴]。请分析根因并给出最小修复。` |
| **性能对比诊断** | `我用 [串行/线程/进程/协程] 跑 [任务描述]，实测耗时 [数据]。请分析是否合理、瓶颈在哪、换哪个执行模型能到多少。` |

## 📊 总进度

进度：░░░░░░░░░░░░░░░░ 0/16 (0%)

| 阶段 | 项目数 | 已完成 |
|:---|:---:|:---:|
| 第一阶段：线程与 GIL 地基 | 4 | 0 |
| 第二阶段：多进程并行 | 3 | 0 |
| 第三阶段：asyncio 异步编程 | 4 | 0 |
| 第四阶段：诊断与设计 | 3 | 0 |
| 第五阶段：综合与前沿 | 2 | 0 |
| **合计** | **16** | **0** |

---

## 🗂️ 第一阶段：线程与 GIL 地基（项目 1-4）

> **目标**：会写线程程序，亲眼见到竞态怎么发生、GIL 到底保护什么不保护什么

### [ ] 项目 1：线程生命周期观察器

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~100 行 |
| **核心知识点** | threading.Thread、start/join、daemon 线程与主线程退出语义、线程交错执行的无序性 |
| **验收标准** | 同一程序连跑 3 次，多个 worker 的输出顺序至少出现 2 种不同排列（证明交错不可预测）；`--daemon` 开关下主线程退出时 daemon 线程的打印被截断（输出行数 < 预期总数），演示"主线程一死 daemon 即被杀" |
| **前置** | 无 |

**🤖 开始提示词**：
> `我要开始 Python 并发项目「线程生命周期观察器」。请给我完整代码约 100 行：用 threading.Thread 演示 start/join/daemon 三种模式，多 worker 并发打印带时间戳的日志展示交错执行，支持 --daemon 命令行开关演示主线程退出时 daemon 被截断，main 中内置验收断言（跑 3 次输出顺序需出现 ≥2 种排列），零第三方依赖，中文注释。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [ ] 项目 2：竞态复现与 GIL 边界实测

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~180 行 |
| **核心知识点** | 竞态条件、读-改-写窗口、GIL 切换检查点只落在回边/调用边界（sys.setswitchinterval 实测）、CPU 密集多线程反而更慢、dis 看字节码 |
| **验收标准** | 裸 `+=` 对照组 100 线程×10000 分毫不差（3.13 切换点不在语句中部）；读-改-写隔调用 20 线程×10000 丢失 >70% 且连跑 5 轮各不相同；双 Barrier 强制 check-then-act 透支到 -500（确定性复现）；CPU 密集 4 线程减速比 ≥1.15×、IO 密集加速比 ≥2.5× |
| **前置** | 项目 1 |

**🤖 开始提示词**：
> `我要开始 Python 并发项目「竞态复现与 GIL 边界实测」。请给我完整代码约 180 行：用 dis 展示 counter += 1 的 LOAD/STORE 两步；对照组裸 += 多线程不丢（3.13 切换点在回边/调用边界）、实验组读-改-写隔一次函数调用复现丢失更新（连跑 5 轮各不相同）；双 Barrier 强制 check-then-act 最坏交错确定性透支；CPU 密集预热后交替 3 轮取中位（4 线程不快于串行，压小切换间隔放大减速比）；IO 密集 4 线程近 4 倍加速，全部断言验收，零第三方依赖。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [ ] 项目 3：同步原语工具箱

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~130 行 |
| **核心知识点** | Lock/RLock、Semaphore 限流、Event 一次性通知、Condition 条件等待、Barrier 集结、with lock 惯用法 |
| **验收标准** | 同一个 counter 场景加 Lock 后 100 线程 × 10000 次递增 = 1_000_000 且连跑 10 次不红；Semaphore(3) 限流下观测到的并发峰值 ≤ 3；Event/Condition/Barrier 三个场景各自断言"等待方确实等到通知/条件/集结点后才继续" |
| **前置** | 项目 2 |

**🤖 开始提示词**：
> `我要开始 Python 并发项目「同步原语工具箱」。请给我完整代码约 130 行：六种同步原语各写一个可运行场景——Lock 修复项目 2 的丢失更新、RLock 演示可重入、Semaphore 限流并监控并发峰值、Event 演示一次性开关、Condition 演示生产者通知消费者、Barrier 演示多线程集结，每个场景末尾放断言（加锁计数恒等、峰值 ≤3 等），零第三方依赖。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [ ] 项目 4：多线程生产者-消费者流水线

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~120 行 |
| **核心知识点** | queue.Queue 线程安全队列、task_done/join、毒丸（sentinel）优雅关闭、maxsize 背压 |
| **验收标准** | 3 生产者 × 2 消费者处理 1000 条任务，处理总数 = 1000 且每条恰好被处理一次（用计数 dict 验证无丢失无重复）；maxsize=10 时观测到队列长度峰值 = 10（背压生效）；发送毒丸后整个程序 2 秒内自行退出且 exit code 为 0 |
| **前置** | 项目 3 |

**🤖 开始提示词**：
> `我要开始 Python 并发项目「多线程生产者-消费者流水线」。请给我完整代码约 120 行：3 生产者线程 + 2 消费者线程通过 queue.Queue 协作处理 1000 条模拟任务，演示 task_done/join 与毒丸关闭两种收尾方式，maxsize 参数演示背压（监控队列峰值长度），用 dict 统计每条任务的消费次数验证无丢失无重复，全部验收点写成断言，零第三方依赖。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

## 🗂️ 第二阶段：多进程并行（项目 5-7）

> **目标**：绕过 GIL，把 CPU 密集任务真正跑满 Apple Silicon 的多核

### [ ] 项目 5：multiprocessing 多核加速实测

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~100 行 |
| **核心知识点** | multiprocessing.Process/Pool、macOS 默认 spawn 启动方式、`if __name__ == '__main__'` 保护、cpu_count、加速比实测 |
| **验收标准** | 4 进程跑同一 CPU 密集任务，加速比 ≥ 2.0× 串行（M 系列芯片实测记录，注意性能核/能效核混合）；去掉 `__main__` 保护在 macOS spawn 下复现启动报错（演示后恢复）；进程池 map 的结果顺序与输入顺序一致（即使 worker 完成乱序） |
| **前置** | 项目 2（知道 GIL 为什么挡路） |
| **⚠️ 风险** | 加速比受芯片型号影响，实测值写入踩坑记录，不达标时先核对 cpu_count 与实际核数 |

**🤖 开始提示词**：
> `我要开始 Python 并发项目「multiprocessing 多核加速实测」。请给我完整代码约 100 行：用 Process 与 Pool 两种方式跑同一 CPU 密集函数（纯 Python 循环），对比串行/2 进程/4 进程耗时并计算加速比，演示 macOS spawn 启动方式下 __main__ 保护缺失的报错（用可恢复的演示函数），验证 pool.map 保序，加速比与保序写成断言，零第三方依赖。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [ ] 项目 6：进程间通信与共享状态

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~130 行 |
| **核心知识点** | Pipe/Queue 消息传递、pickle 序列化成本、SharedMemory 零拷贝共享、Manager 代理与进程锁 |
| **验收标准** | mp.Queue 传 10 万条消息，接收条数 = 100000（消息不丢）；同一 50MB 数组传递，SharedMemory 方式比 pickle + Pipe 快 ≥ 5×（实测记录倍数）；两个进程通过 Manager().dict() 并发更新同一 key 共 10000 次，加锁后最终值恒等于 10000 |
| **前置** | 项目 5 |

**🤖 开始提示词**：
> `我要开始 Python 并发项目「进程间通信与共享状态」。请给我完整代码约 130 行：四种 IPC 各写一个场景——Pipe 双向通信、mp.Queue 批量传 10 万条消息并验证不丢、multiprocessing.SharedMemory 共享 50MB numpy 风格数组（用 array 模块即可，不强制 numpy）对比 pickle 传输耗时、Manager dict 并发更新加锁前后对比，所有验收点写成断言，零第三方依赖。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [ ] 项目 7：concurrent.futures 统一执行器

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~110 行 |
| **核心知识点** | ThreadPoolExecutor/ProcessPoolExecutor 统一接口、Future 状态机、as_completed vs map、异常经 future.result() 传播 |
| **验收标准** | 同一任务函数仅改一个参数即可在 thread/process 执行器间切换并都跑通（参数化断言两种模式结果一致）；as_completed 按完成顺序收集结果（耗时递减的任务序列下，收集顺序确实乱序）；worker 内抛出的异常在主进程 `future.result()` 处原样重现（异常类型断言） |
| **前置** | 项目 3、5 |

**🤖 开始提示词**：
> `我要开始 Python 并发项目「concurrent.futures 统一执行器」。请给我完整代码约 110 行：用 ThreadPoolExecutor 与 ProcessPoolExecutor 跑同一组任务，演示 submit/result、as_completed 按完成顺序收集（用不同耗时的任务制造乱序并断言）、map 按输入顺序返回、worker 异常经 future.result() 抛回主进程并被捕获断言，执行器用工厂函数参数化一键切换，零第三方依赖。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

## 🗂️ 第三阶段：asyncio 异步编程（项目 8-11）

> **目标**：掌握事件循环与协程，写出结构化、可取消、有限流的高并发 IO 程序

### [ ] 项目 8：协程与事件循环机制

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~120 行 |
| **核心知识点** | async/await、协程对象惰性执行、事件循环单线程调度、await 让出控制权、asyncio.run、协程与生成器血缘 |
| **验收标准** | 只调用不 await 的协程不执行任何函数体（调用点前后各打印一次断言"函数体未运行"）；两个协程在单线程内交错执行（打印进程 PID 全部相同 + 输出顺序为固定的交错模式）；`asyncio.run()` 与手动 `loop.run_until_complete()` 产生一致结果 |
| **前置** | 项目 1（先见过线程调度，再对比单线程协作式调度） |

**🤖 开始提示词**：
> `我要开始 Python 并发项目「协程与事件循环机制」。请给我完整代码约 120 行：演示协程对象惰性（不 await 不执行）、asyncio.run 入口、两个协程通过 await sleep 在单线程内交错（打印 PID 证明单线程）、await 让出控制权的时序图（打印时间戳序列）、生成器 yield 与协程 await 的对比小节、手动 loop.run_until_complete 等效写法，全部验收点写成断言，零第三方依赖。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [ ] 项目 9：任务编排——TaskGroup、gather、wait 与取消

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~130 行 |
| **核心知识点** | asyncio.Task、TaskGroup 结构化并发（3.11+）、gather(return_exceptions)、wait 的 FIRST_COMPLETED/FIRST_EXCEPTION、wait_for 超时、CancelledError 传播与清理 |
| **验收标准** | TaskGroup 中一个任务抛异常时其余任务被自动取消，总耗时 < 最长任务完整时长（断言差值 ≥ 50%）；`wait_for` 超时正确抛出 TimeoutError 且子协程的 finally 清理代码被执行过；gather(return_exceptions=True) 在 3 个任务含 1 个失败时仍收齐 3 个结果（1 异常 2 正常） |
| **前置** | 项目 8 |

**🤖 开始提示词**：
> `我要开始 Python 并发项目「asyncio 任务编排」。请给我完整代码约 130 行：演示 TaskGroup 结构化并发（一个失败全部取消，计时断言）、gather 与 return_exceptions 两种模式、asyncio.wait 的三种返回条件、wait_for 超时与 CancelledError 的 try/finally 清理演示，每个场景独立函数 + main 依序运行 + 断言验收，Python 3.13 语法，零第三方依赖。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [ ] 项目 10：异步生产者-消费者与限流

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~120 行 |
| **核心知识点** | asyncio.Queue、异步背压 maxsize、asyncio.Lock/Semaphore、并发上限监控、与线程版 queue.Queue 的异同 |
| **验收标准** | 异步流水线处理 5000 条任务总数守恒（每条恰好一次，dict 计数断言）；Semaphore(10) 包裹下同时在飞的任务数峰值 = 10（监控协程采样断言 ≤10）；queue maxsize=100 时生产端确实被阻塞等待（生产完成时间被消费端拉长，计时断言），对比无背压时队列无限增长 |
| **前置** | 项目 4、9（线程版流水线 + 任务编排） |

**🤖 开始提示词**：
> `我要开始 Python 并发项目「异步生产者-消费者与限流」。请给我完整代码约 120 行：asyncio.Queue 连接多个异步生产者与消费者的流水线处理 5000 条任务，验证总数守恒；用 Semaphore(10) 限制并发并采样监控在飞任务峰值 ≤10；演示 maxsize 背压对生产速率的拉长效应（有/无背压计时对比），与项目 4 线程版的关键差异写进注释，断言验收，零第三方依赖。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [ ] 项目 11：异步 IO 实战——本地批量抓取器

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~150 行 |
| **核心知识点** | aiohttp.ClientSession、连接复用、超时/重试/限流三件套组合、异步并发 vs 线程并发的真实 IO 对比 |
| **验收标准** | 脚本自带在 127.0.0.1 随机端口起 aiohttp 测试服务（50 个端点，模拟 50-200ms 延迟与 30% 随机失败），离线可跑；async 版抓完 50 个 URL（含重试后全部成功）总耗时 < 串行版的 1/5，且 < 同任务线程池版耗时；对失败端点的重试恢复成功率 ≥ 9/10（固定随机种子保证可复现） |
| **技术栈** | aiohttp 3.14.1（✅ 已装实测）；本项目是清单中主线唯一第三方依赖 |
| **前置** | 项目 10 |

**🤖 开始提示词**：
> `我要开始 Python 并发项目「异步本地批量抓取器」。请给我完整代码约 150 行：内置一个 aiohttp 本地测试服务（127.0.0.1 随机端口、50 个端点、模拟延迟与 30% 可控失败、固定随机种子），客户端用 ClientSession + Semaphore(10) + wait_for 超时 + 指数退避重试抓取全部端点，对比串行/线程池/异步三种方式总耗时并断言异步 < 串行 1/5，统计重试恢复成功率 ≥ 9/10，零外部网络依赖。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

## 🗂️ 第四阶段：诊断与设计（项目 12-14）

> **目标**：能复现、定位、修复并发缺陷；能按负载类型选对执行模型

### [ ] 项目 12：死锁与竞态诊断工坊

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~130 行 |
| **核心知识点** | 死锁四条件与循环等待、锁排序（lock ordering）修复法、faulthandler.dump_traceback_later 定位挂起、超时兜底 acquire(timeout=) |
| **验收标准** | 两个线程以相反顺序获取两把锁，程序稳定挂起（复现成功）；`faulthandler.dump_traceback_later(5)` 在挂起 5 秒后 dump 出两个线程各自的栈（输出中能看到两处不同的文件行号，即互相等待点）；按统一锁序修复后，同一场景带 10 秒全局超时连跑 100 次全部正常退出（0 挂起） |
| **前置** | 项目 3 |

**🤖 开始提示词**：
> `我要开始 Python 并发项目「死锁与竞态诊断工坊」。请给我完整代码约 130 行：用两线程反序获取 Lock A/B 稳定复现死锁，挂起时用 faulthandler.dump_traceback_later(5) 自动 dump 全部线程栈并打印等待点，演示 acquire(timeout=) 超时兜底报错，最后给出统一锁序的修复版并断言连跑 100 次无挂起（外层 10 秒守护超时），每个阶段输出到 stdout 便于观察，零第三方依赖。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [ ] 项目 13：并发设计模式集——优雅关闭、退避重试、令牌桶

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~180 行 |
| **核心知识点** | 信号处理（SIGINT/SIGTERM）+ 毒丸的优雅关闭、指数退避 + 抖动、令牌桶限流器、fan-out/fan-in 汇聚 |
| **验收标准** | 用 subprocess 起自己并 `kill -TERM`：子进程完成手头任务后 5 秒内以 exit code 0 退出，且已处理任务数被完整落盘（不丢任务）；令牌桶速率 10/s 连发 100 个请求，10 秒窗口实际通过数在 95-110 之间（±10% 断言）；对 100% 失败的上游，指数退避重试 5 次后放弃，总耗时 ≈ 1+2+4+8（±30% 断言指数曲线） |
| **前置** | 项目 4（毒丸）、11（重试雏形） |

**🤖 开始提示词**：
> `我要开始 Python 并发项目「并发设计模式集」。请给我完整代码约 180 行：三个独立模式各带验收——① signal.signal 处理 SIGTERM/SIGINT + 毒丸队列的优雅关闭（main 用 subprocess 自测 kill -TERM，断言 5 秒内退出且任务落盘不丢）；② 令牌桶限流器（threading 实现，10/s 速率断言 ±10%）；③ 指数退避 + 抖动重试（对必败上游断言尝试次数与总耗时符合指数曲线），另加一个 fan-out/fan-in 汇聚演示，零第三方依赖。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [ ] 项目 14：四种执行模型性能对决

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~150 行 |
| **核心知识点** | 串行/线程/进程/协程四模型同题对比、IO 密集 vs CPU 密集的选型决策、基准测试方法（预热、多轮取中位）、结果可视化 |
| **验收标准** | 同一模拟 IO 任务与同一 CPU 任务分别经四模型实测，三条方向性断言全部成立：CPU 密集下进程版最快、IO 密集下协程版最快、CPU 密集下线程版不快于串行（GIL）；每条断言连跑 3 轮结论方向一致；输出 markdown 对比表 + 可选 matplotlib 柱状图（`--plot` 开关，图存 `images/`） |
| **技术栈** | 标准库为主；matplotlib 3.10.6（✅ 已装，仅 `--plot` 可选用） |
| **前置** | 项目 5、7、10（四模型全部上手过） |

**🤖 开始提示词**：
> `我要开始 Python 并发项目「四种执行模型性能对决」。请给我完整代码约 150 行：同一 IO 模拟任务（time.sleep 模拟延迟）与同一 CPU 密集任务（纯 Python 循环）分别用串行/threading/multiprocessing/asyncio 四模型跑，每模型预热 1 轮 + 正式 3 轮取中位数，输出 markdown 对比表并内置三条方向性断言（进程赢 CPU、协程赢 IO、线程 CPU 不快于串行），可选 --plot 用 matplotlib 出柱状图存 images/，零必需第三方依赖。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

## 🗂️ 第五阶段：综合与前沿（项目 15-16）

> **目标**：把全部能力收进一个可交付系统，并瞥一眼无 GIL 的未来

### [ ] 🏁 项目 15：可切换执行模型的并发下载器

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~350 行 |
| **核心知识点** | 执行器抽象层（sync/thread/process/async 四后端同接口）、限流 + 超时 + 重试组合、断点续传清单、优雅关闭、SHA256 完整性校验、统计报告 |
| **验收标准** | `--model sync/thread/process/async` 四种模式对本地模拟的 50 个文件全部下载成功且 SHA256 逐一校验通过（四模式结果完全一致）；`--limit 5` 并发上限参数在四模型下均生效（在飞峰值 ≤ 5）；运行中 `kill -INT` 后已下载清单落盘，重跑只补剩余文件（第二次日志中"跳过"数 = 已完成数），最终全部文件仍完整 |
| **前置** | 项目 4、7、11、13（流水线、执行器、异步 IO、优雅关闭全部到位） |
| **⚠️ 风险** | 这是终极串联项目，工作量 ≈ 前面 3 个项目，预留一周；先跑通 sync 后端，再逐个加后端 |

**🤖 开始提示词**：
> `我要开始 Python 并发项目「可切换执行模型的并发下载器」。请给我完整代码约 350 行：内置本地 aiohttp/http.server 文件服务（50 个随机内容文件），客户端抽象统一执行器接口，提供 sync/thread/process/async 四个后端一键切换（--model），带 --limit 并发上限、指数退避重试、wait_for 超时、SIGINT 优雅关闭与已完成清单落盘（重跑断点续传），下载后逐文件 SHA256 校验，main 内置全部验收断言（四模式结果一致、上限生效、续传不重下）。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [ ] 项目 16：⚠️ 选做——free-threading 无 GIL 实测与子解释器初体验

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~120 行 |
| **核心知识点** | PEP 703 free-threading 构建、sys._is_gil_enabled() 探测、无 GIL 下真并行多线程、PEP 734 子解释器与 interpreters 模块、与多进程方案的取舍 |
| **验收标准** | free-threading 解释器下 `sys._is_gil_enabled()` 返回 False；项目 2 的无锁 CPU 密集 counter 场景在同一脚本下双构建对比：GIL 构建 4 线程不加速，free-threading 构建加速比 ≥ 1.5×（实测数据写进踩坑记录）；GIL/free-threading 两种构建 × 无锁/加锁四种组合的完整对比表进 stdout 报告 |
| **技术栈** | uv（✅ 已装）+ `uv python install 3.14t` 安装 free-threading 构建 |
| **前置** | 项目 2、5 |
| **⚠️ 风险** | 选做项目，不影响主线毕业；3.14t 已实测装通（uv，arm64），扩展比实测 3.4~3.9× |

**🤖 开始提示词**：
> `我要开始 Python 并发项目「free-threading 无 GIL 实测」。请给我完整代码约 120 行：同一脚本在 GIL 构建与 free-threading 构建下运行对比——启动时打印 sys.version 与 sys._is_gil_enabled()，跑无锁/加锁两种多线程 CPU 密集 counter，输出四组合对比表（构建 × 锁）含加速比，并演示 interpreters（或 _interpreters）子解释器模块的基础用法，脚本用 sys._is_gil_enabled() 自动识别所在构建，断言在 free-threading 下无锁 4 线程加速比 ≥ 1.5×。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

## 📅 周计划

| 周次 | 内容 | 项目数 |
|:---|:---|:---:|
| **第 1 周** | 项目 1-4（线程与 GIL 地基）| 4 |
| **第 2 周** | 项目 5-7（多进程并行）| 3 |
| **第 3 周** | 项目 8-11（asyncio 异步编程）| 4 |
| **第 4 周** | 项目 12-14（诊断与设计）| 3 |
| **第 5 周** | 项目 15 终极串联（+ 项目 16 选做前沿）| 2 |

## 🏆 里程碑

- [ ] **完成项目 1-4** → **线程熟手**：能写线程安全代码，说清 GIL 保护什么、不保护什么
- [ ] **完成项目 5-7** → **多核驾驭者**：能让 CPU 密集任务真正并行，掌握消息传递/共享内存/执行器三种进程协作方式
- [ ] **完成项目 8-11** → **异步工程师**：能独立写出结构化、可取消、有限流的高并发 IO 程序
- [ ] **完成项目 12-14** → **并发诊断专家**：能复现并修复死锁竞态，能按负载类型选对执行模型并给出数据
- [ ] **完成项目 15** → **并发架构师**：能设计可切换执行模型、可优雅关闭、可续传的完整并发系统
- [ ] **完成项目 16（选做）** → **前沿瞭望员**：亲手验证过无 GIL 世界，知道它改变什么、不改变什么

## 📝 每日日志

| 日期 | 项目 | 耗时 | 收获 | 踩坑 |
|:---|:---|:---:|:---|:---|
| | | | | |

## 🔧 环境配置

```bash
# 0. 已就绪（✅ 2026-09-08 本机实测）：
#    Python 3.13.9（anaconda3, arm64）——主线全部够用（TaskGroup/timeout 需 3.11+）
#    aiohttp 3.14.1 / httpx 0.28.1 / requests 2.32.5 —— 项目 11、15 用
#    matplotlib 3.10.6 —— 项目 14 可选出图用
#    uv（~/.local/bin/uv）—— 项目 16 装 free-threading 构建用
# 1. 需安装：主线零新增安装；项目 16 开工时执行：
uv python install 3.14t        # ✅ 已装 3.14.3t（arm64，2026-09-09 实测）；项目 16 已在双构建下跑通
# 2. 每项目开工前：
python3 --version && python3 -c "import aiohttp; print('aiohttp OK')"
```

## ⚠️ 与已有清单的关系

| 已有清单 | 关系 |
|:---|:---|
| `python_interview.md`（已完成 20/20，语言机制面试线） | **互补深潜**：那份覆盖 20 个语言机制考点，其中项目 8「GIL 与并发模型」是并发入门（GIL 概念 + 三模型选型结论）；本清单不重复语言机制题，项目 2 是项目 8 的深潜版（竞态实测 + 加速比数据 + 字节码证据），其余项目均为新覆盖 |
| `interview/08_gil_concurrency/`（五件套实验） | **预习材料**：开工项目 1-2 前可先跑该实验的演示脚本找感觉，本清单项目做完后回头重读它的 README §7 会有完全不同的深度 |
